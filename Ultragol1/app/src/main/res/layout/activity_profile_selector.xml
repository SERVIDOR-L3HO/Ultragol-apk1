<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#080810">

    <!-- Background glow blobs — posicionados creativamente -->
    <View
        android:layout_width="320dp"
        android:layout_height="320dp"
        android:layout_gravity="top|start"
        android:layout_marginStart="-100dp"
        android:layout_marginTop="-100dp"
        android:background="@drawable/gradient_radial_splash"
        android:alpha="0.28"/>

    <View
        android:layout_width="260dp"
        android:layout_height="260dp"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="-70dp"
        android:layout_marginBottom="-70dp"
        android:background="@drawable/gradient_radial_splash"
        android:alpha="0.18"/>

    <View
        android:layout_width="180dp"
        android:layout_height="180dp"
        android:layout_gravity="center|end"
        android:layout_marginEnd="-40dp"
        android:background="@drawable/gradient_radial_splash"
        android:alpha="0.10"/>

    <!-- Main content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <!-- Top bar: logo + gestionar -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:paddingStart="20dp"
            android:paddingEnd="20dp">

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="30dp"
                android:layout_gravity="center_vertical|start"
                android:src="@drawable/logo_ultragol"
                android:scaleType="fitStart"
                android:adjustViewBounds="true"
                android:contentDescription="Ultragol"/>

            <TextView
                android:id="@+id/btnManage"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical|end"
                android:text="Gestionar"
                android:textColor="#FF6B00"
                android:textSize="13sp"
                android:fontFamily="sans-serif-medium"
                android:paddingStart="14dp"
                android:paddingEnd="14dp"
                android:paddingTop="8dp"
                android:paddingBottom="8dp"
                android:clickable="true"
                android:focusable="true"/>
        </FrameLayout>

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#10FFFFFF"/>

        <!-- Title section -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            android:layout_marginTop="38dp"
            android:layout_marginBottom="30dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="¿Quién está viendo?"
                android:textColor="#FFFFFF"
                android:textSize="28sp"
                android:fontFamily="sans-serif-black"
                android:letterSpacing="-0.02"/>

            <!-- Orange underline accent -->
            <View
                android:layout_width="48dp"
                android:layout_height="3dp"
                android:layout_marginTop="12dp"
                android:background="@drawable/profile_save_btn"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:text="Selecciona tu perfil para continuar"
                android:textColor="#55FFFFFF"
                android:textSize="12sp"
                android:fontFamily="sans-serif"
                android:gravity="center"/>
        </LinearLayout>

        <!-- Profile grid -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvProfiles"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:paddingBottom="24dp"
            android:clipToPadding="false"
            android:overScrollMode="never"/>

        <!-- Bottom hint -->
        <TextView
            android:id="@+id/tvManageHint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="36dp"
            android:text="Mantén pulsado un perfil para editarlo"
            android:textColor="#33FFFFFF"
            android:textSize="11sp"
            android:fontFamily="sans-serif"
            android:gravity="center"
            android:drawablePadding="6dp"/>

    </LinearLayout>

</FrameLayout>
