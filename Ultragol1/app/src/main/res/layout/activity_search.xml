<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:orientation="vertical" android:background="@color/bg_primary">
    <LinearLayout android:layout_width="match_parent" android:layout_height="56dp"
        android:orientation="horizontal" android:gravity="center_vertical"
        android:paddingStart="4dp" android:paddingEnd="12dp"
        android:background="@color/bg_surface">
        <ImageView android:id="@+id/btnBack"
            android:layout_width="44dp" android:layout_height="44dp"
            android:src="@android:drawable/ic_menu_revert" android:tint="#FFFFFF"
            android:padding="10dp" android:clickable="true" android:focusable="true"/>
        <EditText android:id="@+id/searchInput"
            android:layout_width="0dp" android:layout_height="40dp" android:layout_weight="1"
            android:layout_marginStart="4dp" android:background="@drawable/search_bg"
            android:paddingStart="14dp" android:paddingEnd="14dp"
            android:hint="Buscar peliculas, series..."
            android:textColor="#FFFFFF" android:textColorHint="#55FFFFFF" android:textSize="14sp"
            android:inputType="textFilter" android:imeOptions="actionSearch"
            android:singleLine="true"/>
    </LinearLayout>
    <ProgressBar android:id="@+id/loadingSearch"
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:layout_gravity="center" android:visibility="gone"
        android:layout_marginTop="40dp"/>
    <TextView android:id="@+id/emptyState"
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:layout_gravity="center" android:text="Busca algo para comenzar"
        android:gravity="center" android:textColor="#55FFFFFF" android:textSize="14sp"
        android:layout_marginTop="60dp"/>
    <androidx.recyclerview.widget.RecyclerView android:id="@+id/resultsGrid"
        android:layout_width="match_parent" android:layout_height="match_parent"
        android:padding="4dp" android:clipToPadding="false"/>
</LinearLayout>
