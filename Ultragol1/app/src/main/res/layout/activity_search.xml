<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_primary">

    <!-- Search bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="4dp"
        android:paddingEnd="12dp"
        android:background="@color/bg_surface">

        <ImageView
            android:id="@+id/btnBack"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:src="@android:drawable/ic_menu_revert"
            android:tint="#FFFFFF"
            android:padding="10dp"
            android:clickable="true"
            android:focusable="true"/>

        <EditText
            android:id="@+id/searchInput"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:background="@drawable/search_bg"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:hint="Buscar peliculas, series, dramas..."
            android:textColor="#FFFFFF"
            android:textColorHint="#55FFFFFF"
            android:textSize="14sp"
            android:inputType="textFilter"
            android:imeOptions="actionSearch"
            android:singleLine="true"/>
    </LinearLayout>

    <!-- Loading bar (TMDB) -->
    <ProgressBar
        android:id="@+id/loadingSearch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"
        android:layout_marginTop="8dp"/>

    <!-- Empty state -->
    <TextView
        android:id="@+id/emptyState"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Busca algo para comenzar"
        android:gravity="center"
        android:textColor="#55FFFFFF"
        android:textSize="14sp"
        android:layout_marginTop="60dp"
        android:visibility="visible"/>

    <!-- Scrollable results -->
    <androidx.core.widget.NestedScrollView
        android:id="@+id/searchScroll"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:overScrollMode="never"
        android:visibility="gone">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- TMDB grid -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/resultsGrid"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="4dp"
                android:clipToPadding="false"
                android:nestedScrollingEnabled="false"/>

            <!-- Shorts Dramas section (shown when there are DM results) -->
            <LinearLayout
                android:id="@+id/shortsSection"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_marginTop="20dp"
                android:visibility="gone">

                <!-- Section header -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:paddingStart="0dp"
                    android:paddingEnd="12dp"
                    android:layout_marginBottom="10dp">

                    <View
                        android:layout_width="4dp"
                        android:layout_height="40dp"
                        android:background="#E91E63"
                        android:layout_marginEnd="12dp"/>

                    <LinearLayout
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:orientation="vertical">

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="🎬 SHORTS DRAMAS"
                            android:textColor="#88FFFFFF"
                            android:textSize="9sp"
                            android:fontFamily="sans-serif-bold"
                            android:textAllCaps="true"
                            android:letterSpacing="0.08"/>

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="EN DAILYMOTION"
                            android:textColor="#FFFFFF"
                            android:textSize="17sp"
                            android:fontFamily="sans-serif-black"
                            android:textAllCaps="true"
                            android:letterSpacing="0.02"/>
                    </LinearLayout>
                </LinearLayout>

                <!-- Horizontal shorts row -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/shortsRow"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingStart="8dp"
                    android:paddingEnd="8dp"
                    android:clipToPadding="false"
                    android:overScrollMode="never"
                    android:nestedScrollingEnabled="false"/>

            </LinearLayout>

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

</LinearLayout>
