<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Permisos adicionales para A7X TV (reproductor, notificaciones, PiP) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Package visibility: detectar apps instaladas (Android 11+) -->
    <queries>
        <package android:name="com.instantbits.cast.webvideo" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="market" />
        </intent>
    </queries>

    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:name=".UltragolApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ultragol"
        android:usesCleartextTraffic="true">

        <!--
            SplashActivity: no portrait lock — TVs are always landscape.
            Both LAUNCHER and LEANBACK_LAUNCHER so it appears in the TV home screen.
        -->
        <activity android:name=".SplashActivity" android:exported="true"
            android:screenOrientation="unspecified"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden|uiMode"
            android:theme="@style/Theme.Ultragol.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            LoginActivity: allow landscape for tablets/TVs.
            TV and tablets handle soft keyboard via adjustResize.
        -->
        <activity android:name=".LoginActivity" android:exported="false"
            android:screenOrientation="unspecified"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Ultragol"/>

        <!--
            MainActivity: fully responsive — portrait on phone, landscape on TV/tablet.
            configChanges includes uiMode so TV ↔ desktop transitions are smooth.
        -->
        <activity android:name=".MainActivity" android:exported="false"
            android:screenOrientation="unspecified"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden|uiMode|smallestScreenSize|screenLayout"
            android:windowSoftInputMode="adjustResize" />

        <activity android:name=".ProfileSelectorActivity" android:exported="false"
            android:screenOrientation="unspecified"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden|uiMode"
            android:theme="@style/Theme.Ultragol"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".SearchActivity" android:exported="false"
            android:screenOrientation="unspecified"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden|uiMode"
            android:windowSoftInputMode="stateVisible|adjustResize" />

        <activity android:name=".DramaShortsActivity" android:exported="false"
            android:screenOrientation="unspecified"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden|uiMode"
            android:windowSoftInputMode="stateVisible|adjustResize" />

        <!--
            ShortsPlayerActivity: portrait on phone, but allow landscape on tablet.
            TVs won't use this screen.
        -->
        <activity android:name=".ShortsPlayerActivity" android:exported="false"
            android:hardwareAccelerated="true"
            android:screenOrientation="unspecified"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:theme="@style/Theme.Ultragol.Fullscreen" />

        <!--
            WatchPartyActivity: synchronized group viewing screen.
        -->
        <activity android:name=".WatchPartyActivity" android:exported="false"
            android:screenOrientation="unspecified"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Ultragol" />

        <!--
            PlayerActivity: sensor (auto-rotate) on all form factors.
        -->
        <activity android:name=".PlayerActivity" android:exported="false"
            android:hardwareAccelerated="true"
            android:screenOrientation="unspecified"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:theme="@style/Theme.Ultragol" />

        <!--
            DetailActivity: portrait on phone, landscape allowed on tablet/TV.
        -->
        <activity android:name=".DetailActivity" android:exported="false"
            android:screenOrientation="unspecified"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden|uiMode"
            android:theme="@style/Theme.Ultragol" />

        <!--
            MediaActivity (ExoPlayer): always landscape. Supports PiP and D-pad.
            configChanges includes uiMode so TV remote navigation doesn't restart it.
            supportsPictureInPicture + configChanges requeridos para PiP de A7X.
        -->
        <activity android:name=".MediaActivity" android:exported="false"
            android:hardwareAccelerated="true"
            android:screenOrientation="sensorLandscape"
            android:supportsPictureInPicture="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|keyboardHidden|uiMode"
            android:theme="@style/Theme.Ultragol.Fullscreen" />

        <!-- Continue Watching Widget -->
        <receiver android:name=".ContinueWatchingWidget"
            android:exported="true"
            android:label="Continuar viendo">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/continue_watching_widget_info"/>
        </receiver>

        <!-- Google Cast SDK options provider -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value=".CastOptionsProvider" />

        <!-- Notifica al usuario cuando termina una descarga (Android DownloadManager) -->
        <receiver android:name=".DownloadCompleteReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE"/>
            </intent-filter>
        </receiver>

        <!-- Boot receiver: reprograma notificaciones al reiniciar -->
        <receiver android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Continue Watching Widget data service -->
        <service android:name=".ContinueWatchingWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />

        <!-- ExoPlayer offline download service -->
        <service android:name=".VideoDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <provider android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false" android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
